{
  "name": "electrobun-pdf",
  "version": "1.0.0",
  "description": "Local-first document editor - edit PDFs and DOCX files, export to PDF",
  "scripts": {
    "sync": "electrobun sync",
    "start": "bun run sync && vite build && electrobun dev",
    "dev": "electrobun dev --watch",
    "dev:hmr": "bun run sync && concurrently \"vite --port 5173\" \"vite build && electrobun dev\"",
    "hmr": "bun run sync && vite --port 5173",
    "build:canary": "bun run sync && vite build && electrobun build --env=canary",
    "build:stable": "bun run sync && vite build && electrobun build --env=stable",
    "typecheck": "bun run sync && tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@tailwindcss/typography": "^0.5.19",
    "@tiptap/extension-color": "^3.0.0",
    "@tiptap/extension-highlight": "^3.0.0",
    "@tiptap/extension-image": "^3.0.0",
    "@tiptap/extension-table": "^3.0.0",
    "@tiptap/extension-table-cell": "^3.0.0",
    "@tiptap/extension-table-header": "^3.0.0",
    "@tiptap/extension-table-row": "^3.0.0",
    "@tiptap/extension-text-align": "^3.0.0",
    "@tiptap/extension-text-style": "^3.0.0",
    "@tiptap/react": "^3.0.0",
    "@tiptap/starter-kit": "^3.0.0",
    "jspdf": "^4.2.1",
    "lucide-react": "^1.0.0",
    "mammoth": "^1.8.0",
    "mupdf": "^1.27.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.0",
    "@types/bun": "latest",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^6.0.0",
    "autoprefixer": "^10.4.20",
    "concurrently": "^10.0.0",
    "electrobun": "2.0.1",
    "postcss": "^8.4.49",
    "tailwindcss": "^4.0.0",
    "typescript": "^7.0.0",
    "vite": "^8.0.0"
  }
}
